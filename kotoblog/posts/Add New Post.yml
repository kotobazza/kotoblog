info:
  name: Add New Post
  type: http
  seq: 1

http:
  method: POST
  url: http://{{baseUrl}}/posts
  body:
    type: json
    data: |-
      {
        "title":"Test1",
        "text":"Test2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
