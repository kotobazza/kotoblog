info:
  name: AddNewPost
  type: http
  seq: 2

http:
  method: POST
  url: http://{{baseUrl}}/posts
  body:
    type: json
    data: |-
      {
        "title":"Test1",
        "text":"Test2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
